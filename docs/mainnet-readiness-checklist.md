# Mainnet Readiness Config Checklist — PERC-8315

_Generated by coder agent audit, 2026-03-31_

## Status Legend
- ✅ PASS — already correct
- ⚠️ ACTION REQUIRED — needs fix before mainnet
- 🔴 BLOCKING — must fix, will break production
- ℹ️ INFO — awareness only

---

## 1. CORS Lockdown

### percolator-api (Railway)
| Check | Status | Detail |
|-------|--------|--------|
| CORS_ORIGINS set in Railway | ✅ PASS | Set to `https://percolatorlaunch.com` |
| No wildcard (`*`) in CORS | ✅ PASS | Uses explicit allowlist |
| www variant covered | ⚠️ ACTION | `https://www.percolatorlaunch.com` is NOT in CORS_ORIGINS — add it |
| NODE_ENV=production | ✅ PASS | Set, exit(1) enforced if CORS_ORIGINS missing |

**Fix:** Add `https://www.percolatorlaunch.com` to Railway `CORS_ORIGINS` env var for percolator-api.

```
CORS_ORIGINS=https://percolatorlaunch.com,https://www.percolatorlaunch.com
```

### percolator-keeper (Railway)
| Check | Status | Detail |
|-------|--------|--------|
| CORS not applicable | ✅ PASS | Keeper health endpoint is not public; no CORS needed |

### Vercel (Frontend)
| Check | Status | Detail |
|-------|--------|--------|
| Next.js middleware CORS | ✅ PASS | `isAllowedOrigin()` in `/api/rpc` allowlists `percolatorlaunch.com`, `www.percolatorlaunch.com` |
| Security headers (HSTS, CSP, X-Frame) | ✅ PASS | All set in `next.config.ts` + `middleware.ts` |

---

## 2. INTERNAL_API_SECRET

### Vercel (Frontend — /api/rpc network guard)
| Check | Status | Detail |
|-------|--------|--------|
| INTERNAL_API_SECRET in Vercel Production env | ⚠️ ACTION | **NOT SET** — cross-network routing will be blocked for all callers |
| Impact if unset | ℹ️ INFO | Safe default: all `?network=` overrides rejected. If Privy dual-chain needs mainnet routing, this MUST be set |

**Fix:** Generate a random secret and add to Vercel Production:
```bash
# Generate
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# Add to Vercel
vercel env add INTERNAL_API_SECRET production
```

---

## 3. skipPreflight Audit

| File | skipPreflight | Context | Status |
|------|--------------|---------|--------|
| `app/lib/tx.ts` | `false` | Main tx send path | ✅ PASS |
| `app/app/api/oracle/advance-phase/route.ts` | `true` | Devnet-only route (exits early on mainnet) | ✅ PASS |
| `app/app/devnet-mint/devnet-mint-content.tsx` | `true` | `/devnet-mint` page — blocked on mainnet by middleware | ✅ PASS |
| `app/hooks/useCloseMarket.ts` | `false` | Admin close market | ✅ PASS |
| `app/hooks/useReclaimSlabRent.ts` | `false` | Admin reclaim | ✅ PASS |
| `scripts/` (various) | mixed | Dev/admin scripts only — never runs in Next.js | ✅ PASS |

**Conclusion:** No production code paths use `skipPreflight: true`. All instances are devnet-only or admin scripts.

---

## 4. Network Environment Variables

### Vercel (Frontend)
| Var | Production Value | Required for Mainnet | Status |
|-----|-----------------|---------------------|--------|
| `NEXT_PUBLIC_DEFAULT_NETWORK` | `devnet` | Must be `mainnet` at launch | 🔴 FLIP AT LAUNCH |
| `NEXT_PUBLIC_SOLANA_NETWORK` | `devnet` | Must be `mainnet` at launch | 🔴 FLIP AT LAUNCH |

**Do NOT flip these until program is ready for mainnet.** When ready:
```bash
vercel env rm NEXT_PUBLIC_DEFAULT_NETWORK production
echo "mainnet" | vercel env add NEXT_PUBLIC_DEFAULT_NETWORK production
vercel env rm NEXT_PUBLIC_SOLANA_NETWORK production
echo "mainnet" | vercel env add NEXT_PUBLIC_SOLANA_NETWORK production
```

### Railway — Keeper
| Var | Value | Status |
|-----|-------|--------|
| `NETWORK` | `mainnet` | ✅ |
| `SOLANA_NETWORK` | `mainnet-beta` | ✅ |
| `RPC_URL` | **`devnet.helius-rpc.com`** | 🔴 BLOCKING — RPC is DEVNET while network=mainnet |
| `FALLBACK_RPC_URL` | `https://api.devnet.solana.com` | 🔴 BLOCKING — fallback also devnet |

> **Note (PERC-8322):** Canonical env var the keeper reads is `RPC_URL` — this flows through `@percolator/shared` `config.rpcUrl` via `validateNetworkConfig()`. `HELIUS_RPC_URL` is a legacy documentation alias; the code does **not** read it. No WS URL env var is needed — keeper does not use WebSocket connections. No hardcoded devnet RPC URLs exist in keeper source code.

**Fix (Khubair action):** Update Keeper Railway env vars to mainnet Helius:
```
RPC_URL=https://mainnet.helius-rpc.com/?api-key=<HELIUS_MAINNET_API_KEY>
FALLBACK_RPC_URL=https://api.mainnet-beta.solana.com
HELIUS_MAINNET_API_KEY=<your_helius_mainnet_key>
```
`HELIUS_MAINNET_API_KEY` is also needed for the Helius Sender API used inside the keeper for transaction submission.

### Railway — API
| Var | Value | Status |
|-----|-------|--------|
| `NETWORK` | `mainnet` | ✅ |
| `NODE_ENV` | `production` | ✅ |
| `CORS_ORIGINS` | `https://percolatorlaunch.com` | ⚠️ Add www |

---

## 5. INDEXER_API_KEY

| Check | Status | Detail |
|-------|--------|--------|
| Set on Vercel Production | ✅ PASS | Present in env list |

---

## 6. Console Logs in Production Code

| Location | Status |
|----------|--------|
| `app/app/api/` routes | ✅ CLEAN — no console.log |
| `app/lib/devnet-signer.ts` | ℹ️ Has audit logs — devnet-only file, blocked on mainnet |
| `app/hooks/useAdvanceOraclePhase.ts` | ℹ️ Has debug logs — devnet-only flow |
| `app/components/providers/AutoFundProvider.tsx` | ℹ️ Has log — devnet auto-fund only |

**Conclusion:** All console.log instances are in devnet-only code paths that are blocked on mainnet by network guard or middleware.

---

## 7. PR Changes in This Audit

### `.env.example` — Added `INTERNAL_API_SECRET` documentation
Operators now know to set this for cross-network RPC routing.

---

## Summary — Actions Required

| Priority | Action | Owner | Location |
|----------|--------|-------|----------|
| 🔴 BLOCKING | Set `RPC_URL` + `FALLBACK_RPC_URL` + `HELIUS_MAINNET_API_KEY` in Keeper Railway vars | Khubair | Railway → percolator-keeper |
| 🔴 BLOCKING (at launch) | Flip `NEXT_PUBLIC_DEFAULT_NETWORK=mainnet` on Vercel | Khubair | Vercel → Production |
| ⚠️ P1 | Add `https://www.percolatorlaunch.com` to `CORS_ORIGINS` | Coder (this PR) | Railway → percolator-api |
| ⚠️ P1 | Generate + set `INTERNAL_API_SECRET` on Vercel Production | Khubair | Vercel → Production |

---

## Non-Issues (Verified)

- ✅ skipPreflight removed from all production tx paths
- ✅ Security headers (HSTS/CSP/X-Frame-Options/X-Content-Type) all in place
- ✅ Rate limiting operational (Upstash Redis on Vercel, in-memory fallback)
- ✅ IP blocklist operational
- ✅ Admin routes protected by Supabase session guard
- ✅ Devnet-only pages blocked by middleware on mainnet deployment
- ✅ `/api/rpc` method allowlist in place — sendTransaction blocked
- ✅ API CORS requires explicit origin on production
